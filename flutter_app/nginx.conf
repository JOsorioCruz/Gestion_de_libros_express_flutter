server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Redirige todas las rutas al index.html para que Flutter maneje el enrutamiento
    location / {
        try_files $uri $uri/ /index.html;
    }
}
